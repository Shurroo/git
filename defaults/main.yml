---

# Don't create a log file
write_log_file: false
# Don't overwrite ~/.gitconfig file if it exists
overwrite_gitconfig: false
# Don't overwrite ~/.ssh/id_rsa file if it exists
overwrite_private_key: false

...
